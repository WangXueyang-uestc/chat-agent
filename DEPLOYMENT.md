# 部署说明

## 问题 1：可以部署到内网吗？

**答案：可以！** ✅

这个应用可以完全部署到内网环境中。以下是部署方法：

### 内网部署步骤

1. **修改服务器绑定地址**

   编辑 `backend/main.py`，将最后一行改为：
   ```python
   uvicorn.run(app, host="0.0.0.0", port=8000, log_level="info")
   ```
   这样服务器会监听所有网络接口，内网其他机器可以访问。

2. **启动服务**

   ```bash
   cd backend
   python main.py
   ```

3. **内网访问**

   - 在同一台机器上访问：`http://localhost:8000`
   - 在内网其他机器上访问：`http://服务器IP:8000`（例如：`http://192.168.1.100:8000`）

4. **注意事项**

   - 确保防火墙允许 8000 端口访问
   - 如果使用代理或反向代理（如 Nginx），可以配置域名访问
   - LLM API 需要能够访问外网（DeepSeek/OpenAI 等），如果内网完全隔离，需要配置代理

---

## 问题 2：能多个用户同时使用而互不影响吗？

**答案：目前不能！** ⚠️

### 当前架构的限制

当前系统**所有用户共享同一个 `config.json` 文件**，这意味着：

- ❌ 多个用户会看到相同的关键内容设置
- ❌ 多个用户会看到相同的 Prompt 模板
- ❌ 多个用户会看到相同的对话列表
- ❌ 用户 A 修改的内容会覆盖用户 B 的内容
- ❌ 用户 A 创建的对话会出现在用户 B 的列表中

### 解决方案

如果需要支持多用户，有以下几种方案：

#### 方案 1：基于会话（Session）的用户隔离（推荐）

为每个浏览器会话分配一个唯一的会话 ID，每个会话有独立的配置文件。

**优点：**
- 实现简单
- 无需用户登录
- 每个浏览器标签页独立

**缺点：**
- 清除浏览器数据会丢失会话
- 不同设备无法共享数据

#### 方案 2：基于用户 ID 的隔离

添加用户认证系统，每个用户有独立的配置文件。

**优点：**
- 数据持久化
- 支持多设备访问
- 可以添加权限管理

**缺点：**
- 需要实现登录系统
- 实现复杂度较高

#### 方案 3：基于数据库的多用户支持

使用数据库（SQLite/PostgreSQL）替代 JSON 文件，按用户 ID 存储数据。

**优点：**
- 支持大量用户
- 数据管理更方便
- 可以添加更多功能（用户管理、权限等）

**缺点：**
- 需要重构数据存储层
- 实现复杂度最高

---

## 快速实现方案 1（会话隔离）

如果您需要快速支持多用户，我可以帮您实现基于会话的隔离。这样：

- ✅ 每个浏览器会话有独立的数据
- ✅ 多个用户同时使用互不影响
- ✅ 无需登录，开箱即用
- ✅ 实现简单，改动最小

需要我帮您实现吗？

---

## 当前使用建议

如果暂时不需要多用户支持，建议：

1. **单用户使用**：适合个人或小团队，按时间段使用
2. **内网部署**：可以部署到内网服务器，团队成员按需使用（但需要协调，避免同时修改）
3. **数据备份**：定期备份 `config.json` 文件，避免数据丢失

